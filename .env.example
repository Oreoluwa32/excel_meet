# Supabase Configuration
REACT_APP_SUPABASE_URL=your_supabase_url_here
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Pusher Configuration (Optional - for real-time messaging)
# Get these from https://pusher.com after creating an account
# If not set, the app will use polling fallback (3-second delay)
REACT_APP_PUSHER_KEY=your_pusher_key_here
REACT_APP_PUSHER_CLUSTER=us2

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit .env to version control (it's in .gitignore)
